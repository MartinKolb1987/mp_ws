
set variable on true if something changed for this route
--> so no query must be triggerd every 500 milliseconds
----> it‘s faster 

// type = checkForNewUpdates
function checkForNewUpdates($route){
	switch($route){
		case 'home':
		case 'whatever':
	}
}